# PY-SCALE
Python application for reading weight data from a serial-connected scale, smoothing live measurements using EMA, detecting stable values, and writing final stable weights to a database with a real-time Tkinter display.
